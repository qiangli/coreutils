# POSIX-required command inventory for Profiles C/D

The canonical 116-command machine-readable inventory is
[`posix-required-commands.tsv`](posix-required-commands.tsv). The complete
generated normative interface reference is
[`posix-required-command-interfaces.md`](posix-required-command-interfaces.md).

Availability is 86 same-name Go applets, 14 shell-only names, and 16 pinned
external providers. Effective Profile C/D selection is separately recorded as
78 Go implementations, 22 shell interfaces, and 16 pinned providers.
