# kb index

7 page(s). Search: `bashy kb search <query>` — check before starting a task; `bashy kb retro` after. Pages live under pages/.

- [pathchk-empty-operands](pages/pathchk-empty-operands.md) `validated/lesson` pathchk treats an empty string as an invalid operand — Quote empty-operand probes explicitly; GNU Coreutils 9.11 and this implementation reject the empty string in default, -p, and -P modes.
- [retained-provider-pins-need-a-separate-active-owner-set](pages/retained-provider-pins-need-a-separate-active-owner-set.md) `validated/lesson` Retained provider pins need a separate active-owner set — When a pinned POSIX external provider migrates to a Go applet, keep the manifest row as a reproducible differential control but separate manifest pins from active dispatch ownership. Registration, dispatch plans, atlas exec/cached effects, generated availability, and owner gates must use the active set; list/check/build and pin-integrity checks may still use every retained row.
- [uuencode-stdin-and-zero-sextet-framing](pages/uuencode-stdin-and-zero-sextet-framing.md) `validated/lesson` uuencode stdin and zero sextet framing — For POSIX uuencode, the header records the input file's access bits and the input '-' operand is a pathname; uudecode treats header '-' and /dev/stdout as standard output, but -o '-' as a pathname. Stage decoded data before overwriting an existing regular file so an input can also name its output.
- [xargs-issue-7-i-replacement-limits](pages/xargs-issue-7-i-replacement-limits.md) `validated/lesson` xargs Issue 7 -I replacement limits — When implementing POSIX Issue 7 xargs -I, enforce that the replacement string appears in no more than five command arguments and that each resulting replacement-bearing argument is at most 255 bytes; -L trailing-blank continuation is a separate rule. Regenerate docs/applet-matrix after adding tests, then run scripts/crossvet.sh.
- [agent-write-reachability-needs-a-real-pty-line](pages/agent-write-reachability-needs-a-real-pty-line.md) `candidate/lesson` agent write reachability needs a real pty line — When registering bashy agent sessions for who/write reachability, publish a bashy-owned pty directory entry that points at the real PTY slave and record that bare line in pkg/who. Keep write/mesg POSIX-inert by selecting the agent registry only when cmds/internal/session sees a bashy agent shell; POSIXLY_CORRECT must retain native utmp and controlling-terminal behavior.
- [build-tag-target-specific-x-sys-queries](pages/build-tag-target-specific-x-sys-queries.md) `candidate/gotcha` Build-tag target-specific x/sys queries — When a Go helper uses x/sys/unix APIs that differ by GOOS, a runtime.GOOS branch does not protect unavailable symbols during cross-compilation. Put each target query in a build-tagged file and keep a conservative fallback for other platforms; verify with the repository crossvet matrix.
- [cobra-hidden-shorthand-aliases-need-explicit-notices](pages/cobra-hidden-shorthand-aliases-need-explicit-notices.md) `candidate/gotcha` Cobra hidden shorthand aliases need explicit notices — When retiring a shorthand flag in Cobra/pflag, ShorthandDeprecated may hide it from help but does not reliably print a user-visible replacement notice in this repo version. Bind a hidden compatibility flag with the shorthand and check cmd.Flags().Changed(<alias>) in RunE when the contract requires a one-line stderr notice.
