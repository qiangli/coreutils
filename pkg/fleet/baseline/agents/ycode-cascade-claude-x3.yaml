agents:
  - name: ycode-cascade-claude-x3
    tool: ycode
    model: sonnet5
    display: "cascade · sonnet5 → opus4.8 (serves L3)"
    band: 3
    band_source: cascade
    base: ycode-sonnet5
    escalation:
      - ycode-opus4.8   # L3 help
    ledger:
      reliability: unmeasured
      notes: |
        X3 cascade, Anthropic lane. Base sonnet5 (L2) does the work; escalates to
        opus4.8 (L3) when the coach's reflex fails to break a loop. The claude-side
        analog of ycode-cascade-x3 (glm→terra).
