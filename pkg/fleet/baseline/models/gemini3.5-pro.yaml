name: gemini3.5-pro
family: gemini
version: "3.5"
band: 3
band_source: declared
# Provisional L3 peg (declared, not measured). gemini3.1-pro was DEMOTED L3->L2 as a
# conductor (9.4x tool-call repeat, never converged — a coder, not a lead). 3.5-pro is
# a newer, stronger model that may clear the conductor bar; this band is a hypothesis to
# be confirmed or corrected by a live conductor/steward bake-off, then re-pegged with
# band_source: measured (or operator).
display: Gemini 3.5 Pro (High)
provider: gemini
kind: subscription
# agy takes DISPLAY strings, not slugs.
model: Gemini 3.5 Pro (High)
quality: 0.85
cost_micro: 5000
